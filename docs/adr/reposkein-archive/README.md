# Archived RepoSkein ADR records

This directory preserves historical machine records from `.reposkein/decisions/`, copied byte-for-byte from the repository's `main` history. The original filenames are retained for reference.

New ADRs are ordinary Markdown files under `docs/adr/`.

